---
title: "Women in space"
description: "A chart providing insights into the history of women that travelled into space."
date: 2021-03-01
cover: "/images/womenspace.png"
coverAlt: "An image of a scatterplot of all women that travelled to space up to 2021"
tags: ["meta", "data viz"]
---

<figure class="viz-wide">
<iframe width="100%" height="683" frameborder="0"
  src="https://observablehq.com/embed/@ralucanicola/women-in-space?cells=svg"></iframe>
</figure>
{/* 
import PlotChart from '~/components/viz/PlotChart.astro';
import MapView from '~/components/viz/MapView.astro';

export const base = import.meta.env.BASE_URL;

This is the first article on the site. The idea is to write short things and
embed the occasional **chart** or **map** when they make a point better than a
paragraph would.

## A sample chart

Below is a tiny Observable Plot chart, rendered client-side. The data is generated
in `public/article-scripts/hello-data.js` — usually I'd load it from a JSON file
under `public/data/` by passing `dataUrl` to `PlotChart`.

<PlotChart id="rain-2024" height={320} caption="Demo: monthly precipitation (mm), made-up numbers." />

<script src={`${base}/article-scripts/hello-data.js`} is:inline defer></script>

## A sample map

A MapLibre map centered on Zürich, with the default demo basemap. Swap in your
own style URL (MapTiler, Mapbox, Protomaps) and a GeoJSON file under
`public/data/` to plot points.

<MapView id="zh-map" lng={8.541} lat={47.376} zoom={11} height={400}
  caption="Demo basemap — replace styleUrl with your own provider for production." />

## Where the pieces live

- Write articles in `src/content/writings/`.
- Drop drawings in `src/content/drawings/`.
- Add images to `public/images/`.
- Add data files to `public/data/`.
- Article-specific JS (chart spec functions) goes in `public/article-scripts/`.
- Toggle the theme using the moon/sun icon in the nav. */}
